This directory contains any resources needed to load the demo which is
embedded in the documentation. The SQL file is used to reset the demo data,
and the service/timer files schedule nightly resets.
